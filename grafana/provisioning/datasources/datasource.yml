apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    orgId: 1
    # Use the name and container port from docker compose
    url: http://prom:9090
    basicAuth: false
    isDefault: true
    editable: true
